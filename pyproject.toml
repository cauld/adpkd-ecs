[project]
name = "adpkd-ecs"
version = "0.1.0"
description = "ADPKD-ECS kill-test pipeline (CNR1 localization; public GEO)"
readme = "README.md"
requires-python = ">=3.12,<3.14"
dependencies = [
    "pandas>=2.2",
    "pyarrow>=17",
    "requests>=2.32",
]

[dependency-groups]
scrna = [
    "anndata>=0.10",
    "scanpy>=1.10",
]

[tool.uv]
package = false
default-groups = []
